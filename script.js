// Minimal script for basic interactions if needed.
// Heavy background animation removed for mobile compatibility and performance.

window.addEventListener('load', () => {
    console.log('Spacecadet site loaded.');
});
